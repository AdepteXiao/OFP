import rsa.RSA
import ui.Menu
import ui.MenuMethods

import scala.util.control.Breaks.break

object Main {
  def main(args: Array[String]): Unit = {
    MenuMethods.run



//     val rsa = RSA.makeFromPQ(
//        BigInt("50000000021"),
//        BigInt("50000000189")
//      )
//      var str = "большой комок грязи"
//      var enc = rsa.encrypt(str)
//      var dec = rsa.decrypt(enc)
//      println(str)
//      println(enc)
//      println(dec)
      }
}